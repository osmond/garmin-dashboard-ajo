import type { Meta, StoryObj } from '@storybook/react';
import MapView from './MapView';

const meta: Meta<typeof MapView> = {
  title: 'Dashboard/MapView',
  component: MapView,
};
export default meta;

export const Default: StoryObj<typeof MapView> = {};
